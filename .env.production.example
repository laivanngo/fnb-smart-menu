# =================================================
# == FILE MÔI TRƯỜNG CHO PRODUCTION (VPS)        ==
# =================================================

# --- Cấu hình Database (PostgreSQL) ---
POSTGRES_USER=myadmin
POSTGRES_PASSWORD=<your_strong_password_here>
POSTGRES_DB=fnb_smart_menu_db
DB_HOST=db
DB_PORT=5432

# --- Cấu hình Backend (FastAPI) ---
SECRET_KEY=<your_super_secret_key_here>

# --- Cấu hình Domain (cho Nginx & Let's Encrypt) ---
# Thay bằng tên miền thực tế của bạn
DOMAIN_ADMIN=admin.yourdomain.com
DOMAIN_API=api.yourdomain.com
DOMAIN_FRONTEND=yourdomain.com

# Email để đăng ký Let's Encrypt (nhận thông báo hết hạn)
LETSENCRYPT_EMAIL=your-email@yourdomain.com

# --- Cấu hình Build-time cho Frontend/Admin (Next.js) ---
NEXT_PUBLIC_API_URL=https://api.yourdomain.com